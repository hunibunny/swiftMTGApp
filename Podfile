# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MTGTracker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MTGTracker
	pod 'Parse'
	target 'AddingParseSDKTests' do
	inherit! :search_paths
	pod 'Parse'
	end

target 'AddingParseSDKUITests' do
	inherit! :search_paths
	pod 'Parse'
	end

end
